public class Demo23_AccessModifiers {
    public static void main(String[] args) {
        // public can be accessed from anywhere
        // private can be accessed only within the same class
        // protected can be accessed within the same package and subclasses
        // default (no modifier) can be accessed only within the same package
        // We can't have to public classes in a single file
    }
}
